<form method="post" action="{{ path('admin_app_menu_delete', {'id': menu.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce menu ? Cette action est irréversible !');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu.id) }}">
    <button class="connect">Supprimer</button>
</form>
