{% extends 'admin/home.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block main %}
    <div class="content">

        <h1>Categorie {{ category.titre }}</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ category.id }}</td>
                </tr>
                <tr>
                    <th>Titre</th>
                    <td>{{ category.titre }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('admin_app_category_index') }}">Retour à la liste</a>

        <a href="{{ path('admin_app_category_edit', {'id': category.id}) }}">Editer</a>

        {{ include('admin/category/_delete_form.html.twig') }}
    </div>
{% endblock %}
