{% extends 'admin/home.html.twig' %}

{% block title %}Edition d'un plat{% endblock %}

{% block main %}
    <div class="content">
        <h1>Modification du plat</h1>

        {{ include('admin/met/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('admin_app_met_index') }}">Retour à la liste</a>

        {{ include('admin/met/_delete_form.html.twig') }}
    </div>
{% endblock %}
