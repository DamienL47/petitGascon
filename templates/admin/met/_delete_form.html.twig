<form method="post" action="{{ path('admin_app_met_delete', {'id': met.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce plat ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ met.id) }}">
    <button class="connect">Supprimer</button>
</form>
